import { createContext } from "react";

export const UnitsContext = createContext(null);

export const UnitsDispatchContext = createContext(null);

export const UnitsProvider = UnitsContext.Provider;
